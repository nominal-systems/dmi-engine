name: Build and Deploy - Staging

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
  
env: 
  DOCKER_IMAGE: diagnostic-modality-integration-engine

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: |
        docker build . \
        --file Dockerfile \
        --target production \
        --tag ${{ env.DOCKER_IMAGE }}:${{ github.sha }}
    - name: List Docker images
      run: |
        docker images
    - name: Push Docker image to Amazon ECR
      uses: appleboy/docker-ecr-action@v0.0.3
      with:
        access_key: ${{ secrets.STAGING_AWS_ACCESS_KEY_ID }}
        secret_key: ${{ secrets.STAGING_AWS_SECRET_ACCESS_KEY }}
        registry: 216821345641.dkr.ecr.us-east-2.amazonaws.com/${{ env.DOCKER_IMAGE }}
        tags: ${{ github.sha }}
        region: us-east-2
    
